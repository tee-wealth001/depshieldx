name: Tests

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: pytest (${{ matrix.os }}, py${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package with dev dependencies
        run: python -m pip install -e ".[dev]"

      - name: Run tests (excludes live-API tests, see the live job below)
        shell: bash
        run: |
          export DEPSHIELDX_CACHE_DIR="${{ runner.temp }}/depshieldx-test-cache"
          python -m pytest -q -rs -m "not live"

  test-live:
    name: pytest live (real npm/PyPI/OSV/GHSA/deps.dev APIs, non-blocking)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install package with dev dependencies
        run: python -m pip install -e ".[dev]"

      - name: Run live-API tests
        shell: bash
        run: |
          export DEPSHIELDX_CACHE_DIR="${{ runner.temp }}/depshieldx-test-cache"
          python -m pytest -q -rs -m live
